node_modules/
dist/
*.tsbuildinfo
.DS_Store
*.log
*.tmp
*.swp
*.bak
*.db
*.sqlite
.vscode/
.idea/
.zed/
.cursor/

# ── Secrets & credentials (NEVER commit) ────────────────────────────────────
.env
.env.*
.env.local
.env.*.local
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
credentials.json
service-account*.json
service_account*.json
**/gcloud-credentials*.json
.npmrc
.pypirc

# ── Local config overrides ───────────────────────────────────────────────────
*.local.json
*.local.yaml
*.local.yml
*.local.toml

# Local-only agent/config files
CLAUDE.md
AGENTS.md

# Generated discovery snapshot. Published as a release asset rather than
# committed — at ~2.8 MB that grows with every provider, a weekly commit put
# ~150 MB of stale data a year into a repo people are meant to clone.
# Stable URL: https://github.com/sriinnu/kosha-discovery/releases/download/snapshot-latest/kosha-latest.json
data/kosha-latest.json
data/kosha-latest.sha256
